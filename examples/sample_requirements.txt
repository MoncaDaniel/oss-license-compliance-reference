# Example Python project requirements -- deliberately spans every risk tier
# so a scan of this file demonstrates the full range of outcomes.

requests==2.31.0        # Apache-2.0 -- permissive, Safe
flask==2.3.0             # BSD-3-Clause -- permissive, Safe
psycopg2==2.9.9          # LGPL-3.0-only -- weak copyleft, context-dependent
mysqlclient==2.2.0       # GPL-2.0-only -- strong copyleft, context-dependent
pyqt5==5.15.9            # GPL-3.0-only -- strong copyleft, context-dependent
some-internal-fork==0.1  # not in the offline dataset -- resolves to Unknown
